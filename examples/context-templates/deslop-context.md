# Deslop — Project Context

## Import Convention

Document your project's import alias convention (e.g. prefer alias imports over deep relative paths).

## Diff Base

If you use stacked PRs (Graphite), the diff base detection in Step 1
(`gt parent --no-interactive 2>/dev/null || echo "main"`) handles this correctly — no additional action needed.
